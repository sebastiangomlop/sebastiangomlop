name: Update README2
on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
jobs:
  build:
    name: Update this repo's README with recent activity
    runs-on: ubuntu-latest

    steps:

      -uses: sebastiangomlop/github-activity-readme@v0.4.0
        
      env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
